name: Build

on:
  workflow_call:
    inputs:
      target:
        required: true
        type: string
        default: dev

jobs:
  build:
    strategy:
      matrix:
        target: [dev, stage, prod]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Build project for ${{ inputs.target }}
        if: ${{ inputs.target }} = ${{ matrix.target }}
        run: echo "Start to project for ${{ inputs.target }}."
